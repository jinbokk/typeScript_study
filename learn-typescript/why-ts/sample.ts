function sum(a: number, b: number): number {
  return a + b;
}

const result = sum(10, 20);

// number 타입의 리턴값에 사용 가능한 여러 메소드를 빠르게 스닙챗으로 사용할 수 있음 
// (VSCode의 인텔리센스 기능 == 코드 자동완성 기능)

result.toString()
result.toLocaleString()